name: atomsbox_music_app_with_bloc
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  atomsbox: ^0.1.0
  audio_handler:
    path: packages/audio_handler
  audio_service: ^0.18.9
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  flutter_bloc: ^8.1.2  
  go_router: ^6.5.5
  intl: ^0.18.0
  just_audio: ^0.9.32
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/songs/